import smtplib
from os.path import basename
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from email.mime.application import MIMEApplication

fromaddr = "nat_N77@mail.ru"
toaddr = "nat_N77@mail.ru"
mypass = "df9klBf4ar5FGHSfhGa"
reportname = "report.xml"

msg = MIMEMultipart()
msg['From'] = fromaddr
msg['To'] = toaddr
msg['Subject'] = "Доброе утро!"

with open(reportname, "rb") as f:
    part = MIMEApplication(f.read(), Name = basename((reportname)))
    part['Content - Desposition'] = 'attachment; filename = "%s"' % basename((reportname))
    msg.attach(part)

body = "Это тест"
msg.attach(MIMEText(body, 'plain'))

server = smtplib.SMTP_SSL('smpt.mail.ru', 465)
server.login(fromaddr, mypass)
text = msg.as_string()
server.sendmail(fromaddr, toaddr, text)
server.quit()